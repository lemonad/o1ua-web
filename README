Prerequisites
-------------
* Git
* Python
* Virtualenv
* Sqlite3 + Python bindings

Getting started
---------------
* Clone github project
* $ cd o1ua-web
  $ virtualenv --no-site-packages .
  $ pip install -r requirements.txt
  $ sqlite refdata.db
    .read schema.sql
    .read example_data.sql (optional)
    .quit
  $ python refdata.py
