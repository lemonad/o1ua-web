To get started:

* Clone git project
* cd into it
* virtualenv --no-site-packages .
* pip install -r requirements.txt
* import documents.sql.example into sqlite db called 'refdata.db'
* python refdata.py

